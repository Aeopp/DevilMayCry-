#ifndef __ENGINE_ENUM_H__
#define __ENGINE_ENUM_H__
BEGIN(ENGINE)

enum MOUSEAXIS
{
	DIM_X,
	DIM_Y,
	DIM_Z,
	MOUSEAXIS_END
};
enum MOUSEBUTTON
{
	DIM_L,
	DIM_R,
	DIM_M,
	MOUSEBUTTON_END
};
enum RENDERID
{
	RENDER_PRIORITY,
	RENDER_NOALPHA,
	RENDER_ALPHA,
	RENDER_UI,
	RENDER_END,
	RENDER_NONE
};
END
#endif // !__ENGINE_ENUM_H__